import { Grid } from '@geist-ui/core';
import React from 'react';

const Resources = () => {
  return (
    <Grid
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Coming Soon
    </Grid>
  );
};

export default Resources;
